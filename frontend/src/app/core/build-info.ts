// GENERADO POR scripts/build-version.js — no editar a mano.
// En desarrollo local queda el valor 'dev' commiteado y el chequeo de versión se apaga.
// El `: string` es obligatorio: sin él TypeScript infiere el tipo literal del timestamp
// y la comparación `BUILD_ID !== 'dev'` deja de compilar (TS2367, "no overlap").
export const BUILD_ID: string = 'dev';
